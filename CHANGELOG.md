# 修复日志

## 2026-08-15 - MD 导航栏 + 胶囊按钮 + FAB（用户「都要」）

### 🎨 界面改进

1. **MD3 导航栏（Navigation Rail）**：顶部 7 个功能 tab 移入左侧导航栏（76px，图标+文字，激活态为 `primary-container` 胶囊）；移动端仍用下拉模块菜单；header 更清爽
2. **主按钮胶囊化**：`.btn-primary` 全量改 `rounded-full`（开始面试/继续/发送/链路检测等），符合 MD 填充按钮形态
3. **MD FAB**：首页右下角新增悬浮扩展按钮「开始面试」（实心蓝 + 投影 + hover 提亮），一键进入实时辅助

## 2026-08-15 - MD 深化：容器色体系 + 设备选择器对齐（用户反馈）

### 🎨 界面改进

1. **MD3 容器色体系**：为 6 套主题新增 `primary/secondary/tertiary-container` + `on-*` + `surface-variant` + `outline-variant` 语义令牌（tailwind 映射 `bg-container-*` / `text-container-on-*`）
2. **应用容器色**：首页模块卡图标块（蓝=primary / 绿=secondary / 琥珀=tertiary 容器）、状态卡图标、激活态 tab（顶部功能、语音/截图模式、移动端模块菜单）全部从 alpha 透明色改为**真·MD tonal 容器**；顶部 tab 与语音/截图 tab 改胶囊形（rounded-full）
3. **修复设备选择器未对齐**：底部「会议音频」列有标签+提示+选择器三行，而「我的麦克风」列只有两行，导致右侧选择器整体偏高 31px——已为右侧（启用/未启用两种状态）补对称提示行并统一 `truncate`，两列选择器与测试按钮现在完全对齐

## 2026-08-15 - MD3 风格收敛：分块 · 简洁 · 干净（用户指定 Material Design 方向）

### 🎨 界面改进

1. **扁平色调（tonal）取代渐变**：首页 Hero / 模块卡 / 状态卡图标块由渐变改纯色/柔和色块（`bg-accent-*/10`），主按钮改实心蓝（去掉蓝→靛渐变），整体更「干净」
2. **MD 浮起卡片**：首页状态卡与模块卡改为「无边框 + 柔和投影」的 elevated card（16px 圆角），去掉了之前「边框 + 阴影」叠加的浑浊感
3. **激活态 tonal**：顶部功能 tab、语音/截图模式 tab、移动端模块菜单的激活态从实心蓝填充改为柔和色块（`bg-accent-blue/10 + 蓝色字 + semibold`），符合 MD 的 tonal 交互语言
4. **清理**：删除未使用的 `.sig-gradient` / `.surface-card` 死样式，签名渐变令牌仅保留给背景光晕与弹层 hairline 使用

## 2026-08-15 - 主题改版：浅色提亮 + 白卡浮起（参照 Stripe/Linear 现代 AI 产品语言）

### 🎨 界面改进

1. **浅色主题从「白底灰框」改为「微蓝灰底 + 白色浮起卡片」**：背景 `#FFFFFF` → `#F7F8FA`（带极淡品牌光晕 radial），卡片/面板改纯白 + 分层柔和投影（`.surface-card`），层次立刻拉开
2. **色彩与对比度**：文字主色 slate-900、次色 slate-600、弱色 slate-500（弱色在浅色底上仍 ≥4.5:1）；强调蓝提亮为 `#2563EB`；主按钮蓝→靛渐变 + 投影
3. **深色主题表面分层**：背景 `#0F0F12` → `#090A0E`，表面 `#12141B`，输入面 `#191C26`，hover `#212531`，层次更分明；文字/强调同步提亮
4. **Editorial Glass / Nord**：纸感主题改为「白卡浮暖纸」；Nord 背景加深、表面提亮
5. **全应用品牌光晕**：`.app-shell` 在左上/右下加极淡蓝→靛 radial bloom，整体有「产品感」而不只是灰

## 2026-08-15 - UI 视觉升级（impeccable 审美批次 1-4）

### 🎨 界面改进

1. **呼吸感（批次 1）**：主控制条去边框改背景分层、间距加宽；ControlBar 收敛——「自动回答 / 答前纠错 / 清空内容」收进 ⚙ 更多弹层，主行只留核心行动；答案/转录面板留白加大（px-4/5 · py-4/5）；顶部导航/辅助子栏 padding 加大
2. **视觉语言（批次 2）**：首页 Hero（品牌渐变图标 + 大标题 + 引导语）；状态条升级为大卡（渐变图标块）；模块卡片图标渐变底 + hover 上浮；新增每主题派生「签名渐变」`--c-grad-*` 令牌，`.sig-gradient/.sig-tile/.sig-soft`；主按钮改蓝→靛渐变；暗色主题表面加 inset 高光
3. **质感（批次 3）**：统一 `.glass-popover` 玻璃浮层（blur + hairline + 大投影 + 顶部分级高光），应用到模块菜单 / 工作台 / 更多弹层
4. **品牌符号（批次 4）**：头部 logo 由通用 Mic 换成专属「面试工作台」对话气泡符号（内联 SVG），配合渐变底
5. **顺带修复**：修复 11 处未生效的 Tailwind 透明度类（`/12`、`/8` → `/10`，如工作台 tab、会话面板、转录角色 chip、看板状态底色等原本透明不可见）

## 2026-08-15 - 评审遗留收尾

### 🎨

1. **灰色系语义化**：KanbanBoard 46 处 gray/white → `text-*/bg-*/border-*` 语义令牌（浅色分支与深色分支统一）；ReviewSessionList 默认状态色 → 中性令牌
2. **暗色主题对比度**：Dark+ `text-muted` 100 116 139 → 148 163 184（对比度 4.05:1 → ~7:1，AA 达标）
3. **悬浮窗令牌化**：新增 `--c-ov-*` 独立 HUD 调色板（8 个令牌，有意不跟随主主题）；截图自检 FAIL 由琥珀改为语义红（与主界面一致），verify 状态全部改用令牌

## 2026-08-15 - UI 评审修复（impeccable critique 驱动）

### 🎨 修复与收敛

1. **P0 可访问性**：ControlBar 关闭粘贴图/结束面试按钮补 aria-label；麦克风测试 dialog 支持 Esc/背景点击关闭；模块菜单补 Esc 关闭；悬浮覆盖层 11-12px 文本对比度提升到 AA（2.3-3.3:1 → 7+）
2. **P1 组件原语**：新增统一 `<Switch>`（36x20，role=switch），设置面板 Toggle 全量换用；`.btn-primary/.btn-danger` 扁平化（去渐变/位移/投影）；StatusBadge 语义化；emoji 图标全部换成 lucide（logo/模式 tab/工作台/toast/视觉标记）；语音/截图按钮补 aria-pressed；移动 tab 补 role=tab
3. **P2 语义令牌**：全仓 282 处 Tailwind 默认色相类替换为 `accent-*` 语义类（复盘/看板/设置/知识库等 19 个文件）；KnowledgeMap 分数色、KanbanBoard 卡片底色去硬编码 hex；分数/风险梯度统一映射 绿/琥珀/红
4. **P3 细节**：`text-[9px]` 18 处提升到 10px；首页卡片标题 15→16px；圆角任意值归一；assist 分隔条 aria 与钳制统一 24-62%；hover 位移/内容平面投影清理
5. **保留例外**：求职看板阶段色板为有意的多色相数据可视化调色板（已在 DESIGN.md 记录）

## 2026-08-14 - UI 设计打磨（impeccable skill 驱动）

### 🎨 界面改进

1. **首页升级为工作台**：状态条（当前会话/简历/JD/知识库/答题模型）+ 6 模块卡片 + 「继续上次准备」
2. **转录区区分角色**：面试官/你自己 角色 chip + 时间戳 + 内联译文更清晰；新增「你的口述」区块
3. **头部图标按钮统一**：统一 32px 样式 + 统一的键盘焦点环
4. **备忘面板**：自动摘要按 Markdown 渲染（标题/列表），不再是裸 `<pre>`
5. **面试工作台三合一**：顶部「工作台」popover 整合 会话 + 备忘 两个 tab（会话支持键盘导航/重命名；备忘可一键固定到右侧常驻面板），头部按钮 8 → 6
6. **设置页 JD 区**：已配置状态徽标 + 「从最近准备空间导入 JD」按钮
7. **可访问性**：全局 `:focus-visible` 焦点环；浅色主题 `text-muted` 对比度提到 ~4.6:1
8. **动效**：转录/备忘面板滑入动画；框选遮罩升级为「选区外变暗 + 十字准线」（原计划放大镜与应用自身的屏幕捕获隐身设计冲突，改为遮罩增强）

## 2026-08-14 - 面试上下文增强 + 实时辅助大版本（成竹实时回答链路）

### ✨ 新增功能

1. **回答 × 简历 / JD 结合**
   - JD 现在会注入实时回答的 prompt（`<jd_context>`），PrepSpace 创建准备空间时自动同步，也可在「设置 → 面试上下文」手动粘贴
   - 新增「回答贴合 JD」开关：开启后回答主动对齐 JD 要求，并用简历真实经历佐证
   - 简历仍按智能规则注入：深挖题强制用简历事实、无关题不强套、明确拒绝时忽略

2. **DeepSeek 前缀缓存预热**
   - 答题前用「系统提示 + 稳定历史」发一个 1-token 请求预热 KV 前缀缓存，真实请求命中 `prompt_cache_hit_tokens`，首字更快
   - 失败静默降级，不阻塞回答链路；可在设置中关闭

3. **滚动面试备忘（Rolling Memo）**
   - 对话压缩升级为结构化三段：已问过的问题 / 我已声称的事实 / 面试官关注点
   - 新增右侧「面试备忘」面板（📓 按钮）：自动备忘只读展示 + 手动备忘可编辑，都会注入回答上下文

4. **自由备注 Notes**
   - 设置页新增自由备注文本框，面试前写要点（公司的坑、想强调的亮点），自动注入 `<notes>`

5. **复盘 / 会话导出**
   - 复盘详情页新增「导出 MD / 导出 JSON」按钮，MD 含整体复盘 + 逐轮问答 + 亮点/风险/评分

6. **转录内联翻译 + 答案语言切换**
   - 「面试官问题内联翻译」开关：问题定稿后并行翻译一句，显示在转录行下方
   - 「回答语言」可切换中文 / English

7. **答后教练建议**
   - 「答后教练建议」开关：答案完成后生成 下一句 / 证据 / 风险 / 自评 四行建议

8. **TTS 语音播报**
   - 「TTS 语音播报答案」开关 + 语速调节，答案完成后自动朗读（浏览器内置语音，零依赖）

9. **拖框选截图（桌面端）**
   - 截图模式新增「框选截图」：透明全屏遮罩，按住左键拖选题目区域，松开自动提交识图解题

10. **多场面试会话隔离**
    - 顶部「会话」按钮：不同公司 / 轮次独立转录、问答、备忘，可新建 / 切换 / 删除 / 重命名

## 2026-06-23 - 面试复盘与启动优化

### 🐛 修复的问题

#### 1. Windows 启动问题
- **问题**：命令行窗口不隐藏，桌面应用不自动打开
- **根因**：`start.py` 在 Windows 上启动时，初始的 Python 进程持有控制台窗口
- **解决方案**：
  - 实现自动重启机制：首次启动显示信息后，以 `CREATE_NO_WINDOW` 标志重启自己
  - 使用 `IA_HIDE_CONSOLE` 环境变量防止无限重启
  - 1 秒延迟确保用户看到启动信息

#### 2. 面试复盘候选人语音记录为空
- **问题**：所有复盘记录中的 `candidate_answer_text` 字段都是空的
- **根因**：`answer_worker.py` 中的 `if not images` 条件阻止了带图片问题打开候选人回答窗口
- **影响**：实际面试中大多数问题都带截图，导致候选人语音转录无法关联到 `qa_id`
- **解决方案**：
  - 移除 `not images` 条件限制
  - 分离关注点：语音记录（总是开启）vs LLM 上下文注入（仅无图片时）
  - 确保所有问题都打开候选人回答窗口

#### 3. 语音识别精确度提升
- **功能**：实现 LLM 驱动的 ASR 纠错
- **实现**：
  - `correct_asr_errors()` 函数使用 LLM 纠正语音识别错误（温度 0.1）
  - 纠正类型：同音字、专业术语、标点符号、语气词
  - 增强验证：
    - 长度检查（不能少于原文 70%）
    - 相似度检查（SequenceMatcher ratio ≥ 0.4）
    - 防止过度修改或幻觉
  - 纠正后的文本保存到数据库，替换原始 ASR 输出

### 📝 修改的文件

#### 启动相关
- **start.py** (行 333-360)
  - 添加 Windows 自动重启逻辑
  - 环境变量守护防止无限循环
  
- **启动.bat** (新增)
  - 简化启动脚本，UTF-8 编码支持

#### 后端核心修复
- **backend/api/assist/answer_worker.py** (行 442-447)
  - 移除 `not images` 限制
  - 添加清晰的注释说明分离的关注点

- **backend/services/storage/review.py** (行 339-380)
  - `update_turn_analysis()` 新增 `corrected_answer` 参数
  - 动态 SQL 构建，减少代码重复

- **backend/services/review_analysis.py** (行 31-99)
  - `correct_asr_errors()` 函数增强验证
  - 添加相似度检查和长度阈值
  - 改进日志记录

- **backend/services/review_async_analysis.py** (行 69-84)
  - 传递纠正后的回答到数据库更新
  - 整场总结使用纠正文本

### ✅ 测试结果

#### 模拟测试
```
✓ 候选人语音记录：从 0 字节 → 30 字节
✓ ASR 纠错：
  - 原始：red地址主要有两种持久化方式，一个是rb地址，一个是AOF
  - 纠正：Redis主要有两种持久化方式，一个是RDB，一个是AOF。
✓ 数据库保存：纠正后文本成功保存
✓ 端到端验证：完整流程通过
```

#### 验证改进
- ✅ 长度阈值从 50% 提升到 70%
- ✅ 新增相似度检查（0.4 阈值）
- ✅ 代码重复减少（SQL 动态构建）
- ✅ 移除冗余的 if-pass 块

### 🎯 使用方法

#### 启动应用
```bash
# Windows: 双击启动脚本
启动.bat

# 或直接运行
python start.py --mode desktop
```

命令行窗口会在 1 秒后自动隐藏，Electron 桌面窗口自动打开。

#### 测试面试复盘
1. 启动实时辅助，开启面试官和候选人双音轨
2. 提问（带截图或纯文本都可以）
3. 候选人语音回答
4. 停止面试，查看复盘
5. 验证：
   - ✓ 候选人回答已记录
   - ✓ 语音识别错误已自动纠正

### 📊 代码审查反馈

**优点**：
- ✅ 核心修复正确解决了问题
- ✅ Windows 启动逻辑符合标准实践
- ✅ 关注点分离清晰（记录 vs LLM 注入）
- ✅ ASR 纠错验证健壮

**改进**：
- ✅ 移除了冗余代码块
- ✅ 增强了 ASR 纠错验证
- ✅ 减少了 SQL 代码重复
- ✅ 改进了日志记录

### 🔮 未来考虑

1. **启动错误日志**：考虑添加日志文件记录隐藏进程的错误
2. **UI 透明度**：在界面上显示"已纠正"标记，让用户知道 ASR 做了修改
3. **纠错指标**：添加纠错成功率统计和监控
4. **成本优化**：每个 turn 都调用 LLM 纠错会产生额外成本，考虑批处理或缓存

### 📈 影响范围

- **后端**：4 个核心文件修改
- **启动**：2 个文件修改
- **测试**：完整端到端验证通过
- **向后兼容**：✅ 不影响现有数据

---

**维护**：成竹维护者  
**审查**：自动化回归与人工复核通过  
**测试状态**：✅ 所有测试通过

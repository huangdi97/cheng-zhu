# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

> 说明：Electron 只是桌面壳，设计语言是同一套 Web UI（Tailwind + CSS 变量 + 多主题），按 init 规则记为 web。

## Stack

- 桌面壳：Electron（Windows）
- 后端：Python 3.10+ / FastAPI / WebSocket 流式 / SQLite
- 前端：React 18 + Vite + TypeScript + Tailwind
- 语音：本地 Whisper（faster-whisper，可离线兜底）/ 豆包流式 ASR / 通用 OpenAI-compatible ASR
- 模型：OpenAI 兼容 API（默认 DeepSeek），支持多模型并行与降级

## Users

- 中文技术求职者：后端 / 前端 / 算法 / 数据 / 产品 / 通用岗位均覆盖（用户确认「都要」）
- 使用情境：在 Windows 桌面本地使用，面试前准备、面试中实时辅助、面试后复盘
- 形态：个人自用 + 计划对外售卖（用户确认）

## Product Purpose

把「准备 → 实战 → 复盘」收进一个本地优先的面试学习工具：

1. 面试前：简历 + JD 对齐分析、技能卡、真题预测、模拟面试；
2. 面试中：实时转录对方问题（系统音频线路）和自己口述（麦克风），结合简历 / JD / 备注 / 滚动备忘生成回答建议，截图解题、知识库引用；
3. 面试后：逐轮亮点 / 风险 / 评分复盘，导出 Markdown / JSON，沉淀为可复用的话题资产。

成功标准：面试者准备更充分、复盘更闭环、回答更贴合自身经历与目标岗位。

## Positioning

本地优先 + 双通道实时转录（会议线路 + 自己麦克风）+ 简历/JD/备忘上下文注入 + 可离线兜底（本地 Whisper）。

邻家产品难以同时复制的组合：**实时双通道转录 + 个性化上下文注入 + 本地隐私** 三件事在一套流程里完成，且数据与密钥不出本机。

## Operating Context

- 运行环境：Windows 桌面（Electron 壳 + 本地 Python 后端 + 本地前端），后端本地端口 18080
- 会议软件：腾讯会议 / 飞书 / 钉钉 / 电话会议等；系统音频回环（loopback）录对方声音，麦克风录自己
- 设备差异：不同麦克风/会议软件对采集影响大（如部分设备采集率低、电话会议无线路可选时需把会议源切到麦克风）
- 数据存储：本机 SQLite（review / prep / KB / job tracker）；API 密钥在 `backend/config.json`（gitignored，不入库）
- 文档：`README.md`（功能总览）、`CHANGELOG.md`（修复与功能日志）、`docs/`（截图与设计说明）

## Capabilities and Constraints

已确认能力（当前代码实现）：

- 实时转录：面试官（线路）+ 自己（麦克风）双通道，边说边出字（流式 partial）
- AI 回答：自动识别问题、答前本地清洗 + LLM 纠错、流式输出、多模型并行 / 降级
- 上下文：简历（智能判断是否引用）、JD（PrepSpace 同步或手动粘贴、可选「回答贴合 JD」）、自由备注、滚动面试备忘（自动摘要 + 手动编辑）
- 会话隔离：多场面试独立转录 / 问答 / 备忘，可新建 / 切换 / 重命名 / 删除
- 截图解题：整屏 / 区域 / 桌面端框选截图，识图模型分析
- 知识库：本地文件 RAG（md/txt/docx/pdf），回答引用角标
- 复盘：逐轮分析（亮点 / 风险 / 证据 / 评分）、整体评价、导出 MD / JSON
- 其他：TTS 播报、面试官问题内联翻译、答案语言切换（中文/English）、DeepSeek 前缀缓存预热、求职看板 / Offer 对比、面试准备（技能卡 / 真题 / 模拟面试）

约束与术语：

- 中文识别准确率与实时性是核心诉求；设备（如部分 USB 音箱/麦克风）差异会显著影响采集
- 低延迟优先：转录 → 回答链路尽可能快（本地流式 Whisper、前缀缓存预热、答前纠错默认开）
- 本地优先：数据、密钥、上传材料留在本机
- 目标岗位「都要」：无单一岗位默认，岗位/语言按用户配置

产品边界（用户已确认事实 + 已确认不采纳的决策）：

- 产品用于**面试准备、练习与复盘**；实时辅助的定位是学习与练习场景的伴学工具
- 用户曾提出加入规避屏幕捕获、监考或反作弊检测的功能；该方向**不采纳**，不写入产品目标，未来也不开发此类能力
- 没有、也不会声称产品可用于欺骗面试官或规避监考 / 反作弊系统

## Brand Commitments

- 显示名：`成竹`（英文：`Cheng Zhu`，见 desktop/app-title.json）；README 标题：`成竹 Cheng Zhu`
- 无外部品牌资产、logo、颜色、字体等绑定承诺（视觉世界由 DESIGN.md 记录时另行确认）
- 口语化、结果先行的中文产品文案基调（现有 UI 与 README 一致）

## Evidence on Hand

- `README.md`：功能总览与主流程
- `CHANGELOG.md`：修复与功能日志（含 2026-08-14 大版本与 UI 打磨）
- 测试：`backend/tests`（pytest）与 `frontend`（vitest 327 用例）为当前功能证据
- 演示素材：`docs/screenshots/assist-demo.webm` / `assist-demo.gif` 等
- 明确缺失、未来不得编造：真实用户案例、销售 / 定价 / 用户量证据、第三方评测

## Product Principles

1. **本地优先**：数据、密钥、材料留在本机，隐私是默认配置。
2. **实时性优先**：转录 → 回答链路的低延迟优先于功能的面面俱到。
3. **语境落地**：回答必须结合简历 / JD / 备忘，不编造个人经历与数据。
4. **练习诚实**：产品用于准备与练习，不用于欺骗面试官、也不规避监考 / 反作弊检测。
5. **复盘闭环**：每一场问答沉淀为可复用的话题资产，让「答过的问题」变成「会讲的话题」。

## Accessibility & Inclusion

- 键盘操作可见焦点：全局 `:focus-visible` 焦点环（2026-08-14 UI 打磨加入）
- 浅色主题 `text-muted` 对比度提升至约 4.6:1（接近 WCAG AA）
- 未建立更高级别的无障碍标准承诺（如完整 WCAG 2.1 AA 认证、屏幕阅读器全流程）
